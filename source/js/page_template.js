'use strict';

var pageTemplate = (function() {
  var init = function(updateFunction) {

  };

  var update = function(data) {
    return true;
  };

  return {
    update: update,
    init: init
  };
}());
