'use strict';

(function() {
  modal.init();
  popup.init();
  urlParser.init( pageTemplate.update );

  // popup.open({message: 'messagecsddfd'});
  // modal.add_photo();
  // modal.add_album();
})();
