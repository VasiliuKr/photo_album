//-
  Миксин для добавления альбома в раздел "Новое в мире" (стр. photo_main)
  @param {albumName} - название альбома
  @param {coverImage} - путь к картинке альбома (первой картинке в альбоме)
  @param {coverUrl} - ссылка с картинки (на попап с слайдером)
  @param {albumUrl} - ссылка альбом
  @param {coverImageTitle} - название первого фото в альбоме
  @param {avatar} - путь к аватарке автора 
  @param {avatarUrl} - ссылка на профиль 
  @param {commentsNumber} - количество комментов
  @param {likesNumber} - количество лайков
  @example
   +recent-albums-item("Прогулки по воде", "assets/img/album-cover.jpg", "#",  "#", "Путешествие на лодке по озеру", "assets/img/avatar.jpg", "#",  "9", "15")

mixin recent-albums-item(albumName, coverImage, coverUrl, albumUrl, coverImageTitle, avatar, avatarUrl, commentsNumber, likesNumber)
  .recent-albums-item
    a(href=`${coverUrl}` class="recent-albums-item__cover-wrapper")
      .recent-albums-item__cover-hover
        +icon('loupe', ['recent-albums-item__cover-icon'])
      img(class="recent-albums-item__cover" src=`${coverImage}` alt="Album cover")
    .recent-albums-item__info-wrapper
      a(href=`${avatarUrl}` class="recent-albums-item__avatar-wrapper")
        .recent-albums-item__avatar-hover
          +icon('more', ['recent-albums-item__avatar-icon'])
        img(class="recent-albums-item__avatar" src=`${avatar}` alt="Avatar")
      .recent-albums-item__info
        h4.recent-albums-item__title-img #{coverImageTitle}
        .recent-albums-item__meta
          a(class="recent-albums-item__comments" href="#")
            +icon('comments', ['recent-albums-item__icon', 'recent-albums-item__icon_comments'])
            .recent-albums-item__comments-number #{commentsNumber}
          a(class="recent-albums-item__likes" href="")
            +icon('like', ['recent-albums-item__icon', 'recent-albums-item__icon_likes'])  
            .recent-albums-item__likes-number #{likesNumber}
    .recent-albums-item__footer
      a(class="recent-albums-item__link" href=`${albumUrl}`)
        +icon('album', ['recent-albums-item__icon', 'recent-albums-item__icon_album'])  
        h3.recent-albums-item__title #{albumName}